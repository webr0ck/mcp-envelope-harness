[pytest]
# Without this, pytest infers rootdir from the collection arguments — running
# `pytest apt/` makes apt/ the rootdir, and the repository-root conftest.py
# (which re-mints an expired labeler leaf) is never loaded.
testpaths = tests apt federation
markers =
    slow: needs a local LLM or real sockets; excluded from a quick run
