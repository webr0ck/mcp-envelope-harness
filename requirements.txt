# Python 3.12+.

# Pinned to a commit: a branch ref would let this repo swap the verifier silently.
# Not a signed tag, so this pins which code you get, not who wrote it.
mcp-trust-verifier @ git+https://github.com/webr0ck/mcp-security-platform.git@583f229e4cb619bafd578de98b00e14126f4a181#subdirectory=sdk/mcp-trust-verifier

# <2: 2.x is breaking, and apt/fastagent_meta_shim.py patches by method name.
mcp>=1.28.1,<2

# Exact: 0.9.22 is the version whose _meta stripping the shim works around.
fast-agent-mcp==0.9.22

# producer/server.py, attacks/mitm.py, federation/*, manual_lab/*
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
pydantic>=2.9

pytest>=8
