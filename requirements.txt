# Python 3.12+ (the verifier requires it).
#
# The verifier is published from the reference implementation's repository and
# pulls in cryptography and jcs (RFC 8785 canonicalisation) itself — the signer
# and the verifier MUST canonicalise identically, so it is deliberately the one
# place those versions are pinned.
mcp-trust-verifier @ git+https://github.com/webr0ck/mcp-security-platform.git#subdirectory=sdk/mcp-trust-verifier

# MCP client/server used by the stdio wire test and the demo servers.
mcp>=1.28.1

# The consumer under test. 0.9.22 is the version whose _meta-stripping defect
# apt/fastagent_meta_shim.py works around; a newer release may not need the shim.
fast-agent-mcp>=0.9.22

# producer/server.py, attacks/mitm.py, federation/*, manual_lab/*
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
pydantic>=2.9

pytest>=8
